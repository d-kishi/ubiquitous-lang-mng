# GitHub Codespaces環境でのDevContainer構築手順

**作成日**: 2025-11-11
**対象**: GitHub Codespaces環境でのClaude Code CLI統合
**目的**: ブランチ切り替え・Codespaces再作成に対応した再現可能な開発環境構築

---

## 📋 目次

1. [概要](#概要)
2. [GitHub Codespaces環境の特徴](#github-codespaces環境の特徴)
3. [Claude Code CLI統合の理由](#claude-code-cli統合の理由)
4. [構築手順](#構築手順)
5. [環境変数設定](#環境変数設定)
6. [DevContainer再ビルド](#devcontainer再ビルド)
7. [動作確認](#動作確認)
8. [ローカルDevContainerとの違い](#ローカルdevcontainerとの違い)
9. [トラブルシューティング](#トラブルシューティング)
10. [参考資料](#参考資料)

---

## 概要

### 目的

GitHub Codespaces環境で**Claude Code CLIを自動インストール**し、以下を実現する：

- ✅ ブランチ切り替えに対応（どのブランチでも動作）
- ✅ Codespaces再作成に対応（手動インストール不要）
- ✅ 再現可能な開発環境（チームメンバー全員が同じ環境）
- ✅ プリビルド機能活用（起動時間短縮）

### 実装方式

**Dockerfileに Claude Code CLI インストールを組み込む**

- コンテナイメージに組み込むため毎回のインストール不要
- ビルド時のバージョン固定が可能
- postCreateCommandで毎回インストールする方式より高速

---

## GitHub Codespaces環境の特徴

### GitHub Codespacesとは

- **定義**: GitHub提供のクラウドベース開発環境（VS Code Web版）
- **特徴**: DevContainerをクラウド上で動作させる
- **利用シーン**: リモート開発・ブラウザからの開発・チーム環境統一

### ローカルDevContainerとの違い

| 項目 | ローカルDevContainer | GitHub Codespaces |
|------|---------------------|-------------------|
| 実行環境 | ローカルDockerコンテナ | GitHub クラウドコンテナ |
| 起動方法 | VS Code「Reopen in Container」 | GitHub「Create codespace」 |
| 永続化 | ローカルディスク | Codespaces永続ストレージ |
| ネットワーク | ローカルネットワーク | GitHub ネットワーク |
| コスト | 無料（ローカルリソース） | 月60時間無料+追加料金 |
| プリビルド | 未対応 | 対応（起動時間短縮） |

### Codespaces特有の考慮事項

1. **タイムアウト設定**: デフォルト30分は短い → 4時間（240分）に延長推奨
2. **永続ストレージ**: `/home/vscode/.claude` は自動永続化
3. **ネットワーク制限**: 一部のネットワーク接続が制限される場合あり
4. **環境変数**: GitHub Secrets経由で安全に設定可能

---

## Claude Code CLI統合の理由

### 背景

**問題**: GitHub Codespaces環境では`claude`コマンドが未インストール

```bash
# Codespaces起動直後
$ claude
/bin/sh: 1: claude: not found
```

### 解決策の選択肢

#### オプションA: Dockerfileに組み込む（✅ 採用）

**メリット**:
- コンテナイメージに組み込まれるため毎回のインストール不要
- ブランチ切り替え・Codespaces再作成に完全対応
- プリビルド機能活用で起動時間短縮
- 高速起動（約3-5分削減）

**デメリット**:
- DevContainer再ビルドが必要（初回のみ）

#### オプションB: postCreateCommandでインストール（❌ 不採用）

**メリット**:
- Dockerfileの変更不要

**デメリット**:
- コンテナ起動ごとにインストール（約30秒～1分の遅延）
- ネットワーク障害時に失敗リスク
- プリビルド機能未活用

#### オプションC: 手動インストール（❌ 不採用）

**デメリット**:
- ブランチ切り替え時に再インストール必要
- Codespaces再作成時に再インストール必要
- 再現性なし

### 最終判断

**Dockerfile組み込み方式を採用** - 再現性・効率性・保守性のバランスが最良

---

## 構築手順

### Step 1: Dockerfile修正

**ファイル**: `.devcontainer/Dockerfile`

**追加箇所**: 33-36行目（既存のPlaywrightインストール）の後

#### 修正前

```dockerfile
# グローバルnpmパッケージインストール
RUN npm install -g \
    # Playwright（E2Eテスト）
    @playwright/test \
    playwright

# Playwrightブラウザインストール（オプション・E2Eテスト用）
RUN npx playwright install --with-deps chromium
```

#### 修正後

```dockerfile
# グローバルnpmパッケージインストール
RUN npm install -g \
    # Playwright（E2Eテスト）
    @playwright/test \
    playwright

# Claude Code CLIインストール（GitHub Codespaces統合）
ARG CLAUDE_CODE_VERSION=latest
RUN npm install -g @anthropic-ai/claude-code@${CLAUDE_CODE_VERSION}

# Playwrightブラウザインストール（オプション・E2Eテスト用）
RUN npx playwright install --with-deps chromium
```

#### 修正内容の説明

1. **ARG CLAUDE_CODE_VERSION=latest**
   - ビルド時にバージョンを指定可能
   - デフォルトは`latest`（最新版）
   - 特定バージョン固定も可能（例: `ARG CLAUDE_CODE_VERSION=1.2.3`）

2. **RUN npm install -g @anthropic-ai/claude-code@${CLAUDE_CODE_VERSION}**
   - グローバルインストール（全ユーザーで利用可能）
   - rootユーザーでインストール（Playwrightと同様）
   - コンテナイメージに組み込まれる

3. **配置場所の理由**
   - Node.js 24.xインストール後（27-30行目）
   - Playwrightインストール直後（33-36行目）
   - 非rootユーザー作成前（41-47行目）

---

## 環境変数設定

### ANTHROPIC_API_KEY設定（必須）

Claude Code CLIの認証に必要な環境変数を設定します。

#### 方法1: GitHub Codespaces Secrets（推奨）

**手順**:

1. **GitHubリポジトリにアクセス**
   ```
   https://github.com/[your-username]/ubiquitous-lang-mng
   ```

2. **Settings → Secrets and variables → Codespaces**
   - 「Secrets」タブを選択
   - 「New repository secret」をクリック

3. **Secret追加**
   - **Name**: `ANTHROPIC_API_KEY`
   - **Value**: `sk-ant-xxxxx...` （ユーザーのAPIキー）
   - 「Add secret」をクリック

4. **Codespaces起動**
   - 新規Codespaces作成 or 既存Codespaces再起動
   - 環境変数が自動的に設定される

**確認方法**:
```bash
# Codespaces内で実行
echo $ANTHROPIC_API_KEY
# 出力: sk-ant-xxxxx... (マスク表示される場合あり)
```

#### 方法2: .env.localファイル（ローカル開発用）

**手順**:

1. **ファイル作成** (`.gitignore`に登録済み)
   ```bash
   # プロジェクトルートで実行
   echo "ANTHROPIC_API_KEY=sk-ant-xxxxx..." > .env.local
   ```

2. **devcontainer.jsonに設定追加** (オプション)
   ```json
   "remoteEnv": {
     "ANTHROPIC_API_KEY": "${localEnv:ANTHROPIC_API_KEY}"
   }
   ```

**注意**: ローカル開発専用・Codespacesでは使用不可

#### 方法3: ~/.bashrc設定（非推奨）

```bash
# ホスト環境で実行
echo 'export ANTHROPIC_API_KEY="sk-ant-xxxxx..."' >> ~/.bashrc
source ~/.bashrc
```

**デメリット**:
- リポジトリ固有の設定ではない
- チームメンバーが各自設定する必要あり

### タイムアウト設定（推奨）

**デフォルト**: 30分（短すぎる）
**推奨**: 240分（4時間）

**手順**:

1. **GitHub Settings → Codespaces**
   ```
   https://github.com/settings/codespaces
   ```

2. **Default idle timeout**
   - 「240 minutes」に変更
   - 「Update」をクリック

---

## DevContainer再ビルド

### GitHub Codespaces環境での再ビルド

#### 新規Codespaces作成（推奨）

**手順**:

1. **既存Codespacesを削除**（オプション）
   - GitHub リポジトリページ → 「Code」ボタン → 「Codespaces」タブ
   - 「...」→ 「Delete」

2. **新規Codespaces作成**
   - 「Code」ボタン → 「Codespaces」タブ
   - 「Create codespace on feature/PhaseB-F2」をクリック
   - DevContainer自動ビルド開始（約5-8分）

3. **ビルド完了待機**
   - プログレスバー表示
   - 完了後、VS Code Web版が自動的に開く

#### 既存Codespacesの再ビルド

**手順**:

1. **Codespacesを開く**
   - 既存Codespacesに接続

2. **コマンドパレット起動**
   - `Ctrl+Shift+P` (Windows/Linux)
   - `Cmd+Shift+P` (Mac)

3. **「Codespaces: Rebuild Container」を選択**
   - 検索欄に「rebuild」と入力
   - 「Codespaces: Rebuild Container」を選択
   - 確認ダイアログで「Rebuild」をクリック

4. **再ビルド完了待機**（約3-5分）

### ローカルDevContainer環境での再ビルド

**手順**:

1. **コマンドパレット起動**
   - `Ctrl+Shift+P` (Windows/Linux)
   - `Cmd+Shift+P` (Mac)

2. **「Dev Containers: Rebuild Container」を選択**
   - 検索欄に「rebuild」と入力
   - 「Dev Containers: Rebuild Container」を選択

3. **再ビルド完了待機**（約3-5分）

---

## 動作確認

### Step 1: Claude Code CLIバージョン確認

**Codespaces/DevContainerターミナルで実行**:

```bash
claude --version
```

**期待される出力**:
```
@anthropic-ai/claude-code/X.Y.Z
```

**エラーが発生した場合**:
```bash
# エラー例
/bin/sh: 1: claude: not found
```
→ [トラブルシューティング](#トラブルシューティング) を参照

### Step 2: 環境変数確認

```bash
echo $ANTHROPIC_API_KEY
```

**期待される出力**:
```
sk-ant-xxxxx...
```

または
```
(空白・何も表示されない)
```
→ [環境変数設定](#環境変数設定) を参照

### Step 3: Claude Code CLI起動

```bash
claude
```

**期待される動作**:
- Claude Code CLIのインタラクティブセッションが起動
- プロンプト表示: `Claude>`

**初回起動時のメッセージ例**:
```
Welcome to Claude Code!
Claude>
```

### Step 4: 簡単なコマンド実行

```bash
# Claude Code内で実行
/help
```

**期待される出力**:
- ヘルプメッセージ表示
- 利用可能なコマンド一覧

### Step 5: セッション終了

```bash
# Claude Code内で実行
/exit
```

または `Ctrl+D`

---

## ローカルDevContainerとの違い

### 実行環境

| 項目 | ローカルDevContainer | GitHub Codespaces |
|------|---------------------|-------------------|
| Docker実行環境 | ローカルDocker Desktop | GitHub managed |
| ネットワーク | ローカルネットワーク | GitHub ネットワーク |
| ストレージ | ローカルディスク | Codespaces永続ストレージ |
| 環境変数 | `.env.local` | GitHub Secrets |

### Claude Code CLI動作

| 項目 | ローカルDevContainer | GitHub Codespaces |
|------|---------------------|-------------------|
| インストール方法 | Dockerfile（同一） | Dockerfile（同一） |
| バージョン | 同一 | 同一 |
| 認証方法 | `.env.local` | GitHub Secrets |
| MCP Server接続 | ローカル | Codespaces内 |

### PostgreSQL接続

| 項目 | ローカルDevContainer | GitHub Codespaces |
|------|---------------------|-------------------|
| 接続先 | `Host=postgres` | `Host=postgres` |
| docker-compose | ローカル実行 | Codespaces実行 |
| PgAdmin | http://localhost:8080 | Codespaces Port Forward |

### 注意事項

- **ポートフォワーディング**: Codespacesは自動的にポートフォワード設定
- **永続化**: `/home/vscode/.claude` は自動永続化（設定保持）
- **パフォーマンス**: Codespacesの方が高速（クラウド環境の性能）

---

## トラブルシューティング

### 問題1: `claude: not found`

**症状**:
```bash
$ claude
/bin/sh: 1: claude: not found
```

**原因**:
- DevContainerが再ビルドされていない
- Dockerfile修正が反映されていない

**解決策**:

1. **Codespaces再ビルド**
   ```
   Ctrl+Shift+P → "Codespaces: Rebuild Container"
   ```

2. **新規Codespaces作成**
   - 既存削除 → 新規作成

3. **Dockerfileの確認**
   ```bash
   cat .devcontainer/Dockerfile | grep -A 2 "Claude Code CLI"
   ```

   期待される出力:
   ```dockerfile
   # Claude Code CLIインストール（GitHub Codespaces統合）
   ARG CLAUDE_CODE_VERSION=latest
   RUN npm install -g @anthropic-ai/claude-code@${CLAUDE_CODE_VERSION}
   ```

---

### 問題2: 環境変数`ANTHROPIC_API_KEY`が設定されていない

**症状**:
```bash
$ echo $ANTHROPIC_API_KEY
(空白)
```

**原因**:
- GitHub Secretsが設定されていない
- Codespaces起動前にSecrets設定が必要

**解決策**:

1. **GitHub Secrets確認**
   - https://github.com/[username]/ubiquitous-lang-mng/settings/secrets/codespaces
   - `ANTHROPIC_API_KEY` が存在するか確認

2. **Secrets設定後、Codespaces再起動**
   - 「Code」→「Codespaces」→「...」→「Stop codespace」
   - 「Start」で再起動

3. **ローカル開発の場合は `.env.local` 作成**
   ```bash
   echo "ANTHROPIC_API_KEY=sk-ant-xxxxx..." > .env.local
   ```

---

### 問題3: DevContainer再ビルドが失敗する

**症状**:
```
ERROR: failed to solve: process "/bin/sh -c npm install -g @anthropic-ai/claude-code@${CLAUDE_CODE_VERSION}" did not complete successfully
```

**原因**:
- npm registryへの接続失敗
- ネットワーク制限
- パッケージバージョン不一致

**解決策**:

1. **ネットワーク確認**
   ```bash
   # Codespacesターミナルで実行
   curl -I https://registry.npmjs.org
   ```

2. **バージョン固定**（Dockerfile修正）
   ```dockerfile
   # latest → 特定バージョン
   ARG CLAUDE_CODE_VERSION=1.2.3
   RUN npm install -g @anthropic-ai/claude-code@${CLAUDE_CODE_VERSION}
   ```

3. **再ビルド再試行**
   ```
   Ctrl+Shift+P → "Codespaces: Rebuild Container"
   ```

---

### 問題4: `claude`コマンドは動作するが認証エラー

**症状**:
```bash
$ claude
Error: ANTHROPIC_API_KEY is not set
```

**原因**:
- 環境変数は設定されているがClaude Code CLIが認識していない
- APIキーの形式が不正

**解決策**:

1. **APIキー形式確認**
   ```bash
   echo $ANTHROPIC_API_KEY | grep -E "^sk-ant-"
   ```

   期待される出力:
   ```
   sk-ant-xxxxx...
   ```

2. **Claude Code CLI設定確認**
   ```bash
   cat ~/.claude/config.json
   ```

3. **手動設定**（最終手段）
   ```bash
   export ANTHROPIC_API_KEY="sk-ant-xxxxx..."
   claude
   ```

---

### 問題5: Codespaces起動時間が長い（10分以上）

**症状**:
- Codespaces起動に10分以上かかる

**原因**:
- プリビルドが有効化されていない
- Dockerfileの最適化不足

**解決策**:

1. **プリビルド有効化**（リポジトリ設定）
   - Settings → Codespaces → Prebuild configuration
   - 「Enable prebuild」

2. **Dockerfile最適化**
   - レイヤーキャッシュ活用
   - 不要なパッケージ削除

3. **初回起動時のみ遅い場合**
   - 2回目以降は高速化（キャッシュ利用）

---

## 参考資料

### 公式ドキュメント

- **GitHub Codespaces**: https://docs.github.com/en/codespaces
- **DevContainers**: https://containers.dev/
- **Claude Code CLI**: https://github.com/anthropics/claude-code

### プロジェクト内ドキュメント

- **DevContainer使用ガイド**: `Doc/99_Others/DevContainer使用ガイド.md`
- **ADR_025**: `Doc/07_Decisions/ADR_025_DevContainer_Sandboxモード統合.md`
- **環境構築手順書**: `Doc/99_Others/EnvironmentSetup/07_Development_Settings.md`

### 技術調査資料

- **Phase B-F2技術調査**: `Doc/08_Organization/Active/Phase_B-F2/Research/`
- **Issue #51検証結果**: `Doc/99_Others/Issue_51_Phase1_検証結果.md`

---

## まとめ

### 実装完了後の状態

✅ **Claude Code CLIが自動インストール**
- ブランチ切り替え対応
- Codespaces再作成対応
- 再現可能な開発環境

✅ **環境変数設定完了**
- GitHub Secrets経由で安全に設定
- ローカル開発でも動作

✅ **DevContainer完全統合**
- 既存の開発環境に影響なし
- .NET SDK、MCP Server、Playwright等すべて動作

### 今後のメンテナンス

- **バージョン更新**: Dockerfileの`CLAUDE_CODE_VERSION`を更新
- **新メンバーのオンボーディング**: このドキュメントを参照
- **トラブル時の対応**: トラブルシューティングセクションを参照

---

**作成日**: 2025-11-11
**最終更新**: 2025-11-11
**関連Issue**: #51（Claude Code on the Web による開発プロセス自動化）
