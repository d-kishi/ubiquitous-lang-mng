# 新規テストプロジェクト作成ガイドライン

**作成日**: 2025-10-13  
**対象読者**: 開発者・SubAgent（tech-research/unit-test/integration-test等）  
**適用範囲**: 新規テストプロジェクト作成時の全工程  
**関連ADR**: ADR_020（テストアーキテクチャ決定）

---

## 🎯 ガイドライン目的・背景

### 目的
- テストアーキテクチャ整合性維持（ADR_020準拠）
- Issue #40類似問題の再発防止
- テストプロジェクト作成標準手順確立
- SubAgentの自律的・正確な作業支援

### 背景
Issue #40（テストアーキテクチャ再構成）の解決過程で以下の課題が顕在化：
- **レイヤー混在**: 全5層が1プロジェクトに集約
- **テストタイプ混在**: Unit/Integration同一プロジェクト
- **言語混在**: F#/C#混在・`EnableDefaultCompileItems=false`による暫定対応
- **重複管理**: Domain層テストが2つのプロジェクトで重複

7プロジェクト構成（ADR_020準拠）を確立し、これを長期維持するための標準手順を策定。

---

## 🔍 事前確認チェックリスト

新規テストプロジェクト作成**前**に以下を必ず確認すること：

### 必須確認事項

- [ ] **テストアーキテクチャ設計書確認**: `/Doc/02_Design/テストアーキテクチャ設計書.md` の内容理解
  - プロジェクト構成図・命名規則・参照関係原則の確認
- [ ] **既存テストプロジェクトとの重複確認**: 同一レイヤー・同一テストタイプのプロジェクト存在確認
- [ ] **レイヤー・テストタイプの分類明確化**: 作成するテストプロジェクトの分類を明確化
  - **Layer**: Domain / Application / Contracts / Infrastructure / Web
  - **TestType**: Unit / Integration / UI / E2E
- [ ] **ADR_020準拠確認**: `/Doc/07_Decisions/ADR_020_テストアーキテクチャ決定.md` の決定事項確認

---

## 📦 プロジェクト作成手順

### Step 1: プロジェクト作成

#### F#プロジェクト（Domain/Application層）

```bash
# 基本構文
dotnet new xunit -lang F# -n {ProjectName}.{Layer}.{TestType}.Tests -o tests/{ProjectName}.{Layer}.{TestType}.Tests

# 例1: Domain層Unit Tests
dotnet new xunit -lang F# -n UbiquitousLanguageManager.Domain.Unit.Tests -o tests/UbiquitousLanguageManager.Domain.Unit.Tests

# 例2: Application層Unit Tests
dotnet new xunit -lang F# -n UbiquitousLanguageManager.Application.Unit.Tests -o tests/UbiquitousLanguageManager.Application.Unit.Tests
```

#### C#プロジェクト（Contracts/Infrastructure/Web層）

```bash
# 基本構文
dotnet new xunit -n {ProjectName}.{Layer}.{TestType}.Tests -o tests/{ProjectName}.{Layer}.{TestType}.Tests

# 例1: Infrastructure層Unit Tests
dotnet new xunit -n UbiquitousLanguageManager.Infrastructure.Unit.Tests -o tests/UbiquitousLanguageManager.Infrastructure.Unit.Tests

# 例2: Infrastructure層Integration Tests
dotnet new xunit -n UbiquitousLanguageManager.Infrastructure.Integration.Tests -o tests/UbiquitousLanguageManager.Infrastructure.Integration.Tests
```

---

### Step 2: 命名規則確認

**必須命名規則**: `{ProjectName}.{Layer}.{TestType}.Tests`

#### Layer選択
- **Domain**: ドメインモデル・Value Objects・Domain Services
- **Application**: Use Cases・Application Services
- **Contracts**: DTOs・Type Converters（F#↔C#境界）
- **Infrastructure**: Repositories・EF Core・外部連携
- **Web**: Blazor Components・Pages・Web Services

#### TestType選択
- **Unit**: 単体テスト（テスト対象レイヤーのみ参照）
- **Integration**: 統合テスト（複数レイヤー・DB・外部連携）
- **UI**: UIコンポーネントテスト（Blazor bUnit使用）
- **E2E**: エンドツーエンドテスト（Playwright使用）

#### 命名規則確認チェックリスト
- [ ] `{ProjectName}` = `UbiquitousLanguageManager` 確認
- [ ] `{Layer}` = Domain/Application/Contracts/Infrastructure/Web のいずれか
- [ ] `{TestType}` = Unit/Integration/UI/E2E のいずれか
- [ ] `.Tests` サフィックス確認
- [ ] ディレクトリ名とプロジェクト名の一致確認

---

### Step 3: 言語・SDK選択

#### 言語選択原則

| Layer | 言語 | 理由 |
|-------|------|------|
| Domain | F# | ドメインロジックはF#で実装 |
| Application | F# | ユースケースはF#で実装 |
| Contracts | C# | DTOs・Type ConvertersはC#で実装 |
| Infrastructure | C# | EF Core・RepositoriesはC#で実装 |
| Web | C# | Blazor ServerはC#で実装 |

#### SDK選択原則

| TestType | SDK | 理由 |
|----------|-----|------|
| Unit | `Microsoft.NET.Sdk` | 標準テスト（デフォルト） |
| Integration | `Microsoft.NET.Sdk` | 標準テスト |
| UI (bUnit) | `Microsoft.NET.Sdk.Razor` | **Blazor Componentテストのため必須** |
| E2E (Playwright) | `Microsoft.NET.Sdk` | 標準テスト |

#### .csproj/.fsproj 手動編集（必要な場合）

**UI Tests（bUnit）の場合のみ、SDK変更が必要**:

```xml
<!-- 修正前 -->
<Project Sdk="Microsoft.NET.Sdk">

<!-- 修正後 -->
<Project Sdk="Microsoft.NET.Sdk.Razor">
```


---

## 🔗 参照関係設定

### ADR_020参照関係原則

各テストプロジェクトは**テスト対象レイヤーとその依存層のみ**を参照する。

### Unit Tests参照関係

**原則**: テスト対象レイヤーのみ参照

#### Domain.Unit.Tests（F#）

```xml
<ItemGroup>
  <!-- ADR_020準拠: Unit Tests原則 - テスト対象レイヤーのみ参照 -->
  <ProjectReference Include="..\..\src\UbiquitousLanguageManager.Domain\UbiquitousLanguageManager.Domain.fsproj" />
</ItemGroup>
```

#### Application.Unit.Tests（F#）

```xml
<ItemGroup>
  <!-- Application層はDomain層に依存するため、両方参照 -->
  <ProjectReference Include="..\..\src\UbiquitousLanguageManager.Application\UbiquitousLanguageManager.Application.fsproj" />
  <ProjectReference Include="..\..\src\UbiquitousLanguageManager.Domain\UbiquitousLanguageManager.Domain.fsproj" />
</ItemGroup>
```

#### Contracts.Unit.Tests（C#）

```xml
<ItemGroup>
  <!-- Contracts層はApplication・Domain層に依存するため、3層参照 -->
  <ProjectReference Include="..\..\src\UbiquitousLanguageManager.Contracts\UbiquitousLanguageManager.Contracts.csproj" />
  <ProjectReference Include="..\..\src\UbiquitousLanguageManager.Application\UbiquitousLanguageManager.Application.fsproj" />
  <ProjectReference Include="..\..\src\UbiquitousLanguageManager.Domain\UbiquitousLanguageManager.Domain.fsproj" />
</ItemGroup>
```

### Integration Tests参照関係

**原則**: 必要な依存層のみ参照（WebApplicationFactory使用時は全層参照）

#### Infrastructure.Integration.Tests（C#）

```xml
<ItemGroup>
  <!-- 統合テスト: 全層参照（WebApplicationFactory使用のため） -->
  <ProjectReference Include="..\..\src\UbiquitousLanguageManager.Infrastructure\UbiquitousLanguageManager.Infrastructure.csproj" />
  <ProjectReference Include="..\..\src\UbiquitousLanguageManager.Application\UbiquitousLanguageManager.Application.fsproj" />
  <ProjectReference Include="..\..\src\UbiquitousLanguageManager.Domain\UbiquitousLanguageManager.Domain.fsproj" />
  <ProjectReference Include="..\..\src\UbiquitousLanguageManager.Web\UbiquitousLanguageManager.Web.csproj" />
  <ProjectReference Include="..\..\src\UbiquitousLanguageManager.Contracts\UbiquitousLanguageManager.Contracts.csproj" />
</ItemGroup>
```

### UI Tests参照関係

**原則**: Web層のみ参照（bUnit推奨）

#### Web.UI.Tests（C#）

```xml
<ItemGroup>
  <!-- UIテスト: Web層のみ参照（bUnitベストプラクティス） -->
  <ProjectReference Include="..\..\src\UbiquitousLanguageManager.Web\UbiquitousLanguageManager.Web.csproj" />
</ItemGroup>
```

### 参照関係チェックリスト

- [ ] **テスト対象レイヤー参照確認**: Unit Testsはテスト対象レイヤーのみ（最小化原則）
- [ ] **不要な参照の削除確認**: 循環依存防止・ビルド時間最小化
- [ ] **ADR_020参照関係原則準拠確認**: テストタイプ別の参照関係原則に準拠
- [ ] **WebApplicationFactory使用時の全層参照確認**: Integration/E2E Testsで必要な場合

---

## 📦 NuGetパッケージ追加

### Unit Tests（F#）標準パッケージ

```bash
# Domain.Unit.Tests / Application.Unit.Tests 作成後
cd tests/UbiquitousLanguageManager.{Layer}.Unit.Tests

# xUnit本体
dotnet add package xunit
dotnet add package xunit.runner.visualstudio
dotnet add package Microsoft.NET.Test.Sdk

# F#テストユーティリティ
dotnet add package FsUnit.xUnit

# カバレッジ測定
dotnet add package coverlet.collector
```

**実装例（Domain.Unit.Tests.fsproj）**:

```xml
<ItemGroup>
  <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.11.1" />
  <PackageReference Include="xunit" Version="2.9.0" />
  <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2" />
  <PackageReference Include="coverlet.collector" Version="6.0.2" />
  <PackageReference Include="FsUnit.xUnit" Version="6.0.1" />
</ItemGroup>
```

### Unit Tests（C#）標準パッケージ

```bash
# Contracts.Unit.Tests / Infrastructure.Unit.Tests 作成後
cd tests/UbiquitousLanguageManager.{Layer}.Unit.Tests

# xUnit本体
dotnet add package xunit
dotnet add package xunit.runner.visualstudio
dotnet add package Microsoft.NET.Test.Sdk

# アサーション・モック
dotnet add package FluentAssertions
dotnet add package Moq

# カバレッジ測定
dotnet add package coverlet.collector
```

### Integration Tests標準パッケージ

```bash
# Infrastructure.Integration.Tests 作成後
cd tests/UbiquitousLanguageManager.Infrastructure.Integration.Tests

# xUnit本体
dotnet add package xunit
dotnet add package xunit.runner.visualstudio
dotnet add package Microsoft.NET.Test.Sdk

# 統合テスト用パッケージ
dotnet add package Microsoft.AspNetCore.Mvc.Testing      # WebApplicationFactory
dotnet add package Microsoft.EntityFrameworkCore.InMemory # InMemoryDB
dotnet add package Testcontainers.PostgreSql              # Testcontainers

# カバレッジ測定
dotnet add package coverlet.collector
```

### UI Tests（bUnit）標準パッケージ

```bash
# Web.UI.Tests 作成後（Sdk="Microsoft.NET.Sdk.Razor" 必須）
cd tests/UbiquitousLanguageManager.Web.UI.Tests

# bUnit本体
dotnet add package bunit
dotnet add package bunit.web

# xUnit本体
dotnet add package xunit
dotnet add package xunit.runner.visualstudio
dotnet add package Microsoft.NET.Test.Sdk

# アサーション・モック
dotnet add package FluentAssertions
dotnet add package Moq
```

### E2E Tests（Playwright）標準パッケージ

```bash
# E2E.Tests 作成後
cd tests/UbiquitousLanguageManager.E2E.Tests

# xUnit本体
dotnet add package xunit
dotnet add package xunit.runner.visualstudio
dotnet add package Microsoft.NET.Test.Sdk

# Playwright
dotnet add package Microsoft.Playwright

# WebApplicationFactory（必要に応じて）
dotnet add package Microsoft.AspNetCore.Mvc.Testing
```


---

## ✅ ビルド・実行確認手順

プロジェクト作成完了後、以下の手順で必須確認を実施すること。

### Step 1: ソリューションファイル更新

```bash
# 新規テストプロジェクトをソリューションに追加
dotnet sln add tests/{ProjectName}.{Layer}.{TestType}.Tests

# 例: Domain.Unit.Tests追加
dotnet sln add tests/UbiquitousLanguageManager.Domain.Unit.Tests

# ソリューションファイル確認
dotnet sln list
```

**期待結果**: 新規プロジェクトがソリューション一覧に表示される

### Step 2: ビルド確認

```bash
# 新規テストプロジェクト個別ビルド
dotnet build tests/{ProjectName}.{Layer}.{TestType}.Tests

# 成功基準: 0 Warning/0 Error
```

**エラー発生時の確認ポイント**:
- 参照関係の正確性（ProjectReference確認）
- NuGetパッケージの依存関係エラー
- F#/C#言語選択の誤り（F#プロジェクトにC#ファイル混在等）

### Step 3: 全体ビルド確認

```bash
# ソリューション全体ビルド
dotnet build

# 成功基準: 0 Warning/0 Error
```

**重要**: 新規プロジェクト追加により、既存プロジェクトのビルドが壊れていないことを確認。

### Step 4: テスト実行確認

```bash
# 新規プロジェクト個別実行（空テスト・サンプルテスト）
dotnet test tests/{ProjectName}.{Layer}.{TestType}.Tests

# 成功基準: テスト実行成功（0件でもOK・サンプルテスト追加推奨）

# 全体テスト実行
dotnet test

# 成功基準: 全テスト100%成功
```

### ビルド・実行確認チェックリスト

- [ ] `dotnet sln add` 実行完了
- [ ] `dotnet sln list` で新規プロジェクト確認
- [ ] 新規プロジェクト個別ビルド成功（0 Warning/0 Error）
- [ ] ソリューション全体ビルド成功（0 Warning/0 Error）
- [ ] 新規プロジェクト個別テスト実行成功
- [ ] ソリューション全体テスト実行成功（100%維持）

---

## 📚 ドキュメント更新手順

プロジェクト作成完了後、以下のドキュメント更新を必須とする。

### 必須更新対象

#### 1. テストアーキテクチャ設計書更新（15分）

**ファイル**: `/Doc/02_Design/テストアーキテクチャ設計書.md`

**更新内容**:
- **プロジェクト構成図**: 新規プロジェクト追加
- **プロジェクト一覧表**: 新規プロジェクトの行追加
  - プロジェクト名
  - Layer
  - TestType
  - 言語（F#/C#）
  - 主要NuGetパッケージ
  - 参照関係

#### 2. README.md更新（10分）

**ファイル**: `/README.md`（プロジェクトルート）

**更新内容**:
- **テスト実行手順**: 新規プロジェクト個別実行コマンド追加

**更新例**:

```markdown
### テスト実行

# 全テスト実行
dotnet test

# レイヤー別実行
dotnet test tests/UbiquitousLanguageManager.Domain.Unit.Tests
dotnet test tests/UbiquitousLanguageManager.Application.Unit.Tests
dotnet test tests/UbiquitousLanguageManager.NewLayer.Unit.Tests  # 新規追加

# テストタイプ別実行
dotnet test tests/UbiquitousLanguageManager.*.Unit.Tests
dotnet test tests/UbiquitousLanguageManager.*.Integration.Tests
```

### ドキュメント更新チェックリスト

- [ ] テストアーキテクチャ設計書更新完了（プロジェクト構成図・一覧表）
- [ ] README.md更新完了（テスト実行手順追記）
- [ ] git commit作成（ドキュメント更新のみの独立commit推奨）

---

## 🚨 再発防止チェックリスト（Issue #40教訓）

新規テストプロジェクト作成時、Issue #40類似問題の再発を防止するため、以下を必ず確認すること。

### 技術負債回避

- [ ] **EnableDefaultCompileItems=false設定禁止**
  - プロジェクト作成時にこの設定を追加しない
  - .csproj/.fsprojに `<EnableDefaultCompileItems>false</EnableDefaultCompileItems>` が存在しないこと確認
  - **理由**: Issue #40でF#/C#混在環境でのビルドエラー暫定対応として使用され、技術負債化

- [ ] **F#/C#混在回避**
  - F#プロジェクトにC#ファイルを含めない
  - C#プロジェクトにF#ファイルを含めない
  - **理由**: F#コンパイラはC#ファイルをコンパイルできない（過去に7件のC#→F#変換が必要だった）

- [ ] **テストタイプ混在回避**
  - 1プロジェクト内に複数テストタイプを混在させない
  - Unit Tests / Integration Tests / UI Tests / E2E Tests は個別プロジェクト
  - **理由**: テスト実行粒度制御不可・CI/CD最適化困難

- [ ] **レイヤー混在回避**
  - 1プロジェクト内に複数レイヤーのテストを混在させない
  - ADR_020のレイヤー別分離原則準拠
  - **理由**: 責務分離の原則違反・影響範囲不明確・保守性低下

### 設計原則確認

- [ ] **レイヤー別分離確認**
  - 1プロジェクト = 1レイヤー × 1テストタイプ
  - プロジェクト名が `{ProjectName}.{Layer}.{TestType}.Tests` 形式

- [ ] **参照関係最小化**
  - Unit Testsはテスト対象レイヤーのみ参照
  - 不要な参照を追加しない（ビルド時間増加・循環依存リスク）
  - ADR_020参照関係原則に準拠

- [ ] **命名規則準拠**
  - `{ProjectName}.{Layer}.{TestType}.Tests` 形式厳守
  - Layer: Domain/Application/Contracts/Infrastructure/Web
  - TestType: Unit/Integration/UI/E2E

### 品質確認

- [ ] **ビルド確認**
  - 新規プロジェクト個別ビルド成功（0 Warning/0 Error）
  - ソリューション全体ビルド成功（既存プロジェクトへの影響なし）

- [ ] **テスト実行確認**
  - 新規プロジェクト個別テスト実行成功
  - ソリューション全体テスト実行成功（既存テスト100%維持）

- [ ] **ADR_020準拠確認**
  - テストアーキテクチャ設計書との整合性確認
  - 7プロジェクト構成との整合性確認

---

## 📋 クイックリファレンス（チートシート）

### プロジェクト作成コマンド一覧

```bash
# F# Domain Unit Tests
dotnet new xunit -lang F# -n UbiquitousLanguageManager.Domain.Unit.Tests -o tests/UbiquitousLanguageManager.Domain.Unit.Tests

# F# Application Unit Tests
dotnet new xunit -lang F# -n UbiquitousLanguageManager.Application.Unit.Tests -o tests/UbiquitousLanguageManager.Application.Unit.Tests

# C# Contracts Unit Tests
dotnet new xunit -n UbiquitousLanguageManager.Contracts.Unit.Tests -o tests/UbiquitousLanguageManager.Contracts.Unit.Tests

# C# Infrastructure Unit Tests
dotnet new xunit -n UbiquitousLanguageManager.Infrastructure.Unit.Tests -o tests/UbiquitousLanguageManager.Infrastructure.Unit.Tests

# C# Infrastructure Integration Tests
dotnet new xunit -n UbiquitousLanguageManager.Infrastructure.Integration.Tests -o tests/UbiquitousLanguageManager.Infrastructure.Integration.Tests

# C# Web UI Tests（SDK変更必須）
dotnet new xunit -n UbiquitousLanguageManager.Web.UI.Tests -o tests/UbiquitousLanguageManager.Web.UI.Tests

# C# E2E Tests
dotnet new xunit -n UbiquitousLanguageManager.E2E.Tests -o tests/UbiquitousLanguageManager.E2E.Tests
```

### NuGetパッケージ追加コマンド一覧

```bash
# F# Unit Tests標準パッケージ
dotnet add package xunit && dotnet add package xunit.runner.visualstudio && dotnet add package Microsoft.NET.Test.Sdk && dotnet add package FsUnit.xUnit && dotnet add package coverlet.collector

# C# Unit Tests標準パッケージ
dotnet add package xunit && dotnet add package xunit.runner.visualstudio && dotnet add package Microsoft.NET.Test.Sdk && dotnet add package FluentAssertions && dotnet add package Moq && dotnet add package coverlet.collector

# Integration Tests標準パッケージ
dotnet add package Microsoft.AspNetCore.Mvc.Testing && dotnet add package Microsoft.EntityFrameworkCore.InMemory && dotnet add package Testcontainers.PostgreSql

# UI Tests（bUnit）標準パッケージ
dotnet add package bunit && dotnet add package bunit.web

# E2E Tests（Playwright）標準パッケージ
dotnet add package Microsoft.Playwright
```

### 確認コマンド一覧

```bash
# ソリューション追加
dotnet sln add tests/{ProjectName}

# ソリューション一覧確認
dotnet sln list

# ビルド確認
dotnet build tests/{ProjectName}           # 個別
dotnet build                               # 全体

# テスト実行確認
dotnet test tests/{ProjectName}            # 個別
dotnet test                                # 全体

# カバレッジ測定
dotnet test --collect:"XPlat Code Coverage"
```

---

## 📚 関連ドキュメント

- **ADR_020**: `/Doc/07_Decisions/ADR_020_テストアーキテクチャ決定.md` - 設計決定の根拠
- **テストアーキテクチャ設計書**: `/Doc/02_Design/テストアーキテクチャ設計書.md` - プロジェクト構成図・参照関係
- **組織管理運用マニュアル**: `.claude/rules/operations/organization-manual.md` - プロセス遵守チェックリスト

---

## 🔄 更新履歴

| 日付 | 更新内容 | 更新者 |
|------|---------|-------|
| 2025-10-13 | 初版作成 | Claude Code |

---

**作成者**: Claude Code
**承認者**: プロジェクトオーナー
