@{
    ViewData["Title"] = "ホーム";
}

@* ホームページ（リダイレクト専用） *@
@* 注意: 未認証ユーザーは自動的にログイン画面へリダイレクトされるため、このコンテンツは表示されません *@

<div class="container py-5">
    <div class="row">
        <div class="col-lg-8 mx-auto text-center">
            <div class="spinner-border text-primary" role="status">
                <span class="visually-hidden">リダイレクト中...</span>
            </div>
            <p class="mt-3">画面を切り替えています...</p>
        </div>
    </div>
</div>

@* 以下のコンテンツは削除: 未認証ユーザーは自動リダイレクトのため表示されない *@